[project]
name = "petfish-skill-builder"
version = "0.3.0"
requires-python = ">=3.9"

[dependency-groups]
dev = ["jsonschema>=4.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-v --tb=short"
