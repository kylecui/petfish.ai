[project]
name = "project-initializer-skill"
version = "1.0.0"
description = "Safe project initializer skill for OpenCode/Antigravity"
requires-python = ">=3.10"
dependencies = ["jinja2"]

[tool.ruff]
line-length = 100
target-version = "py310"
