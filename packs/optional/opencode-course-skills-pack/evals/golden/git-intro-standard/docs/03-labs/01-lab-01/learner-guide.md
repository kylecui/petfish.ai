# 实验一：本地仓库与分支合并（学员手册）

## 实验目标

独立完成仓库初始化、三次提交、一次分支创建与合并。

## 环境说明

- Git 2.40+，任一终端
- 无需网络

## 操作步骤

1. `git init lab-repo` 并进入目录
2. 提交三个文件变更，提交信息遵循"动词 + 对象"格式
3. 创建 `feature` 分支，修改文件后合并回主分支
4. 用 `git log --oneline --graph` 截图记录结果

## 验收标准

- `git log` 至少 4 个提交
- 分支图可见一次合并
- 无未跟踪文件残留
